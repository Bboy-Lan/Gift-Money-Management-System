export const CURRENT_RELEASE_NOTES = `## 0.4.1 - 2026-08-19

### 修复问题
- 修复多标签筛选将标签拼接为普通文本的问题，改为按标签名精确匹配任一已输入标签。

### 兼容性说明
- 礼金库、礼金簿、历史记录、管理员配置和 \`webview2-fixed\` 不受影响。

---

## 0.4.0 - 2026-08-19

### 新增功能
- 礼金明细新增统一筛选面板，支持按人物、地址、备注、标签、支付方式、金额、日期和回礼条件筛选。
- 筛选结果同步更新礼金笔数、最高金额和礼金总额，并在启动时以非强制提示提醒可用更新。

### 修复与改进
- 统一使用同一批筛选记录驱动表格和统计，避免筛选后统计仍显示整本礼金簿数据。
- 版本号升级为标准 SemVer 功能版本，关于页面仅保留最近三个版本的更新明细。

### 兼容性说明
- 礼金库、礼金簿、历史记录、管理员配置和 \`webview2-fixed\` 不受影响。

---

## 0.3.87 - 2026-08-13

### 修复问题
- 修复在线更新安装完成后使用错误旧路径重启，导致程序退出后无响应的问题。
- 安装成功后优先启动标准安装目录中的新版本程序，并记录安装与启动结果。

### 改进
- 持续登记默认开启，按钮明确显示“已开启/已关闭”，并在切换礼金库和页面时保留状态。
- 设置页移除重复身份说明，右上角实时显示用户模式或管理员模式。
- 关于页更新明细改为真正可折叠，压缩重复介绍区域和本机模式弹窗空白。

### 兼容性说明
- 礼金库、礼金簿、历史记录、管理员配置和 \`webview2-fixed\` 不受影响。

---

## 0.3.86 - 2026-08-13

### 新增功能
- 本机模式新增持续登记；每条礼金保存成功后自动打开下一条登记表单，取消或关闭即可退出。

### 兼容性说明
- 礼金库、礼金簿、历史记录、管理员配置和 \`webview2-fixed\` 不受影响。`;
